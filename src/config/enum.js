/**控制select的默认值 */
const selectEnum = {
  DEFAULT_VALUE:undefined,//默认值
}

/**控制dialog显示 */
const showDialogEnum = {
  HIDE:-1,
  SHOW_ADD:-2
}

/**各属性初始化值 */
const propertyInitEnum = {
  OBJECT:null,
  NUMBER:-1,
  STRING:'',
  ARRAY:[],
  BOOLEAN:false,
}

export{
  selectEnum,
  showDialogEnum,
  propertyInitEnum
}